import java.util.Stack;

public class Storage {
	public static Stack<Double> stack = new Stack<>();

	public static void Hashma(String arguments) {
		Storage.stack.push(arguments); // без ошибок
	}
}
